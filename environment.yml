name: navit-rf
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.1
  - torchvision>=0.16
  - cudatoolkit=12.1
  - pip
  - pip:
      - -e .
      - matplotlib>=3.8
      - pyyaml>=6.0
